import{_ as t,o,c as a,ao as n}from"./chunks/framework.BRVGVHVL.js";const p=JSON.parse('{"title":"Physics notes","description":"","frontmatter":{},"headers":[],"relativePath":"physics_notes.md","filePath":"physics_notes.md","lastUpdated":null}'),i={name:"physics_notes.md"};function s(r,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="Physics-notes" tabindex="-1">Physics notes <a class="header-anchor" href="#Physics-notes" aria-label="Permalink to &quot;Physics notes {#Physics-notes}&quot;">​</a></h1><p>Where GEMB&#39;s implementation departs from, or interprets, the published law it is cited to, and the reasoning behind choices a user of the model would otherwise have to read the source to find. Organized by subject. Every scheme&#39;s full citation is in the docstring of the function that implements it.</p><h2 id="Densification" tabindex="-1">Densification <a class="header-anchor" href="#Densification" aria-label="Permalink to &quot;Densification {#Densification}&quot;">​</a></h2><p><strong><code>:Arthern</code></strong> (Arthern et al., 2010) is the default: <code>dρ/dt = c(ρᵢ − ρ)</code> in two stages either side of 550 kg m⁻³, driven by mean accumulation and an Arrhenius factor in mean annual temperature.</p><p><strong><code>:ArthernB</code></strong> is the stress-and-grain-size form of the same paper (eq. B1), <code>dρ/dt ∝ σ/r²</code>. Overburden is the integral <code>Σ(ρⱼdzⱼ + waterⱼ)g</code> over the cells above, not the nearest cell&#39;s density applied to the whole overlying depth. Cross-validated against the Community Firn Model&#39;s <code>Arthern2010T</code> to 1 part in 10⁸. Because the rate goes as <code>1/r²</code>, this is the scheme most sensitive to grain radius, and therefore to <code>grain_growth_method</code>. The steady-state initial guess falls back to <code>:Arthern</code>, which spinup then relaxes.</p><p><strong><code>:GSFC2020</code></strong> is Medley et al. (2022) GSFC-FDM v1.2.1 eq. 18, the recalibrated successor to <code>:Arthern</code>: the same relaxation form with mean accumulation raised to a fitted exponent (α₀ = 0.91, α₁ = 0.644) and a per-stage activation energy (59 500 and 56 870 J mol⁻¹ against Arthern&#39;s single 60 000). Because α ≠ 1 the accumulation units are load-bearing rather than absorbed into a prefactor: accumulation is kg m⁻² yr⁻¹, the convention the exponents were fitted against. Cross-checked against the Community Firn Model to a 0.102% gravity offset.</p><p><strong><code>:Simonsen2013</code></strong> is Simonsen et al. (2013), <code>:Arthern</code>&#39;s form and activation energies retuned for Greenland: a constant factor 0.8 below 550 kg m⁻³, and 1.25·γ above with <code>γ = 61.7/√A · exp(−3800/RT̄)</code>, γ scaling the second stage only. The form follows Lundin et al. (2017) FirnMICE eqs. A36–A37, which publishes no numeric tuning scalars, so 0.8 and 1.25 are the Community Firn Model&#39;s values, against which the implementation is cross-checked to the same gravity offset. As for <code>:GSFC2020</code> the accumulation units are load-bearing, since <code>γ ∝ A^(−1/2)</code>.</p><p><strong><code>:Crocus</code> and <code>:CrocusPure</code></strong> are the viscous settling of Vionnet et al. (2012) eqs. 5–9, <code>dD/D = −σ/η·dt</code> with <code>η = f1·f2·η₀(ρ/c_η)·exp(a_η(T_fus − T) + b_η·ρ)</code>. This is the only scheme in which liquid water affects densification: <code>f1 = (1 + 60·W_liq/(ρ_w·D))⁻¹</code> reduces viscosity by up to ~61× in wet cells, and <code>f2 = min(4, exp(min(0.4 mm, gs − 0.2 mm)/0.1 mm))</code> raises it for coarse angular grains. Overburden is the <code>Σ(ρdz + water)·g</code> integral taken at the cell midpoint — the paper&#39;s half-own-weight rule for the surface layer, applied uniformly. Integration uses <code>D·exp(−σ/η·dt)</code>, eq. 5&#39;s exact solution at constant σ and η.</p><p>Two departures from the published law:</p><ul><li><p><code>f2</code> is floored at 1. Eq. 9 is bounded above but not below, so it <em>softens</em> fine-grained snow — 0.368, a 2.7× speedup, at GEMB&#39;s 0.05 mm fresh-snow radius. Crocus never reaches that regime because eq. 9 applies only to non-dendritic snow, whose grain size the paper puts at 0.3–0.4 mm where eq. 9 gives 2.7–4. GEMB carries one grain radius for both regimes, so the floor keeps <code>f2</code> a stiffening correction over the domain eq. 9 covers and inert below it.</p></li><li><p><code>:Crocus</code> hands cells at or above 450 kg m⁻³ to <code>:GSFC2020</code>. Eq. 7 is fitted to a 1–2 m alpine snowpack and <code>exp(b_η·ρ)</code> saturates in firn: unblended, the law gives 0.7–0.8× <code>:Arthern</code>&#39;s compaction rate in the top few metres but only 0.02–0.09× below 20 m. 450 kg m⁻³ is the threshold the Community Firn Model uses for the same purpose. The handover is a branch rather than a weighted blend, since neither paper prescribes a blending function. <code>:CrocusPure</code> applies eq. 5 at every density.</p></li></ul><p>The paper&#39;s <code>c_η = 250</code> and its <code>f2</code> are used, where the Community Firn Model hardcodes <code>f2 = 4</code> and adopts van Kampenhout et al. (2017)&#39;s retuned <code>c_η = 358</code>.</p><p><strong><code>:Barnola1991</code></strong> is Herron and Langway (1980) stage 1 below 550 kg m⁻³ and Barnola et al. (1991) pressure sintering above, <code>dρ/dt = ρ·A₀·exp(−Q/RT)·f(ρ)·σ³</code> with <code>A₀ = 2.54e4 MPa⁻³ s⁻¹</code> and <code>Q = 60 kJ mol⁻¹</code>. It is the only scheme here that treats the firn–ice transition mechanistically rather than extrapolating a snow/firn fit: <code>f(ρ)</code> is a polynomial fitted to Pimienta and Duval (1987) below 800 kg m⁻³ and the analytic isolated-spherical-pore form <code>(3/16)(1 − ρ/ρᵢ)/(1 − (1 − ρ/ρᵢ)^⅓)³</code> above, which vanishes with porosity, so the law self-limits as ρ → ρᵢ rather than being driven to zero by a <code>(ρᵢ − ρ)</code> factor. Overburden and integration follow <code>:Crocus</code> and forward Euler respectively. Stage 1 is bit-for-bit <code>:HerronLangway</code>, sharing one kernel. Cross-checked against the Community Firn Model, which agrees on both branches and all four polynomial coefficients.</p><p>Four constraints and departures:</p><ul><li><p>The self-limiting behavior holds only if the closed-pore branch is reached, and the 800 kg m⁻³ handover is absolute while the polynomial carries no <code>ρᵢ</code>. <code>:Barnola1991</code> therefore requires <code>density_ice &gt;= 900</code>, enforced at construction, against <code>[800, 950]</code> elsewhere.</p></li><li><p>The paper&#39;s driving stress is overburden <em>minus bubble pressure</em>; only the overburden is applied, so effective stress is overstated above 830 kg m⁻³ where pores close. The paper gives no expression for the bubble term. Goujon et al. (2003) eqs. A11–A12 do, so it belongs in a separate scheme.</p></li><li><p>Only the closed-pore branch scales with <code>density_ice</code>, and the branches meet in value at ρᵢ = 919.96 and in slope at 920.06 against the paper&#39;s stated C¹ matching, so the fit assumes ρᵢ ≈ 920. At GEMB&#39;s default 910 the rate steps down 14% crossing 800 kg m⁻³ — a discontinuity in <code>dρ/dt</code>, not in ρ.</p></li><li><p>The 550 kg m⁻³ handover is also a rate discontinuity, by construction in the paper: it joins a depth-independent accumulation-driven rate to a σ³ one. On a 250 K, 300 kg m⁻² yr⁻¹ column, sintering is ~1.3e4× slower than Herron–Langway just above 550 at 1 m depth and overtakes it only near 13 m.</p></li></ul><p><code>n = 3</code> throughout, as in the paper, whose <code>A₀</code> was fitted against it over 0.55–0.8 g cm⁻³. The paper&#39;s <code>n = 1</code> remark scopes itself to bulk ice below close-off (Pimienta and Duval torsion-tested 0.85 g cm⁻³ ice and report no densification rate), so it is not an unimplemented firn branch. Calibrated over −14 to −57 °C and 2.2–65 g cm⁻² yr⁻¹, with no liquid-water term.</p><p><strong>Accumulation forcing.</strong> Accumulation-driven schemes (<code>:Arthern</code>, <code>:Ligtenberg</code>, <code>:Simonsen2013</code>, <code>:GSFC2020</code>, <code>:HerronLangway</code>, <code>:Crocus</code>, and <code>:Barnola1991</code> below its stage transition) compact against <code>accumulation_mean</code>, which partitions precipitation on <code>rain_temperature_threshold</code>, since rain does not bury the column. This is the same split <code>initialize_climate_summary</code> applies, so the transient run and the initializer use the same accumulation flux. <code>densification_accumulation = :precipitation</code> drives compaction with total precipitation instead; on a site with 3.2% rain the difference in 32-year final mean column density is ~1.1 kg m⁻³.</p><p><strong>Mean temperature.</strong> <code>exp(E/RT)</code> is convex in <code>1/T</code>, so <code>exp(E/R⟨T⟩) ≠ ⟨exp(E/RT)⟩</code> — at a site with a ±15 K seasonal amplitude about 250 K the two differ by tens of percent, in the direction of under-densification. <code>mean_temperature_method = :arrhenius</code> uses the effective temperature</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>T_eff = E_g / (R · log(mean(exp(E_g / (R·T_air)))))</span></span></code></pre></div><p>with <code>E_g = 42 400 J mol⁻¹</code>. One <code>T_eff</code> is exact for all three consumers (<code>:ArthernB</code>, <code>:GSFC2020</code>, and Simonsen&#39;s γ) only because they share that activation energy; a scheme with a different <code>E_g</code> needs its own. The default <code>:arithmetic</code> uses the plain mean.</p><p><strong>Domain calibration.</strong> <code>:Ligtenberg</code>&#39;s <code>M0</code>/<code>M1</code> multipliers carry nine calibration sets across Antarctica and Greenland (<code>densification_lookup.jl</code>). They are not interchangeable with other models&#39; fits, which were regressed against different forcing products and a different Arrhenius reference temperature.</p><h2 id="Grain-growth" tabindex="-1">Grain growth <a class="header-anchor" href="#Grain-growth" aria-label="Permalink to &quot;Grain growth {#Grain-growth}&quot;">​</a></h2><p>Grain metamorphism runs on every timestep, independently of <code>albedo_method</code>. Four schemes read <code>grain_radius</code> — <code>:ArthernB</code>, <code>:Crocus</code> and <code>:CrocusPure</code> densification, and the <code>:grain_radius_threshold</code>/<code>:grain_radius_w_threshold</code> emissivity methods — and each selects independently of the albedo scheme.</p><p><code>grain_growth_method</code> governs the dry non-dendritic branch:</p><ul><li><p><strong><code>:Marbouty</code></strong> — Marbouty (1980) temperature-gradient metamorphism. Its density factor is identically zero above 400 kg m⁻³ and its gradient factor is meaningless where <code>∇T → 0</code>, so it does not evolve grain radius in deep firn.</p></li><li><p><strong><code>:Arthern</code></strong> — Arthern et al. (2010) normal growth, <code>dr²/dt = k_gr·exp(−E_g/RT)</code>, density-independent. This is the default and what the Community Firn Model uses.</p></li><li><p><strong><code>:hybrid</code></strong> — Marbouty below 400 kg m⁻³ and Arthern at or above it, keeping the temperature-gradient physics in seasonal snow while grains continue to coarsen in firn.</p></li></ul><p>The dendritic and wet (Brun, 1989) branches and the <code>GRAIN_DIAMETER_MAX</code> cap are common to all three. Because <code>:ArthernB</code> densification goes as <code>1/r²</code>, faster grain growth densifies <em>more</em> slowly: on synthetic forcing with <code>:ArthernB</code>, final mean column density is 902.6 kg m⁻³ under <code>:Marbouty</code> (median grain radius 0.41 mm), 891.4 under <code>:hybrid</code> (0.589 mm) and 889.3 under <code>:Arthern</code> (0.592 mm).</p><h2 id="Fresh-snow" tabindex="-1">Fresh snow <a class="header-anchor" href="#Fresh-snow" aria-label="Permalink to &quot;Fresh snow {#Fresh-snow}&quot;">​</a></h2><p><code>new_snow_method</code> selects the fresh-snow density:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Option</th><th style="text-align:right;">Form</th></tr></thead><tbody><tr><td style="text-align:right;"><code>:Constant350</code> (default), <code>:Constant315</code>, <code>:Constant150</code></td><td style="text-align:right;">Bare constants; 350 is what the Community Firn Model ships</td></tr><tr><td style="text-align:right;"><code>:Fausto</code></td><td style="text-align:right;">315 kg m⁻³, the Fausto et al. (2018) regression at 256.2 K</td></tr><tr><td style="text-align:right;"><code>:FaustoFit</code></td><td style="text-align:right;"><code>362.1 + 2.78·(T_air − 273.15)</code>, the regression itself, clamped to <code>[1, ρᵢ]</code> since it reaches zero near 143 K</td></tr><tr><td style="text-align:right;"><code>:Pahaut</code></td><td style="text-align:right;"><code>max(50, 109 + 6·(T_air − 273.15) + 26·√U)</code></td></tr><tr><td style="text-align:right;"><code>:Kaspers</code>, <code>:KuipersMunneke</code></td><td style="text-align:right;">Temperature-dependent polar fits</td></tr></tbody></table><p><code>:Pahaut</code> is Pahaut (1975) as implemented in Crocus, via Lafaysse et al. (2026) eq. 35. It is the only option fitted to alpine seasonal snow rather than to a polar ice sheet, and it is much lighter than the polar fits over their common range — 107 kg m⁻³ at −10 °C and 5 m s⁻¹ against <code>:FaustoFit</code>&#39;s 334 — because alpine snowfall is warmer, wetter, and far less wind-packed than the katabatic-scoured surfaces the Greenland and Antarctic fits were regressed on. Prefer it for temperate and mid-latitude glaciers, where the polar fits overestimate fresh-snow density, suppressing new-snow albedo and speeding its burial. The published 50 kg m⁻³ floor is retained; it binds below about −10 °C in calm air.</p><p><code>:Fausto</code>, <code>:FaustoFit</code> and <code>:Pahaut</code> also select the Crocus (Vionnet et al., 2012) wind-dependent fresh-grain dendricity and sphericity, those being what Crocus itself pairs these densities with. <code>fresh_snow_density</code> takes instantaneous air temperature and wind speed where they are available and the climatological means in the steady-state march, so the initial guess is well defined at every setting.</p><h2 id="Thermal-physics" tabindex="-1">Thermal physics <a class="header-anchor" href="#Thermal-physics" aria-label="Permalink to &quot;Thermal physics {#Thermal-physics}&quot;">​</a></h2><p><strong>Heat capacity.</strong> <code>heat_capacity_method = :constant</code> holds <code>heat_capacity_ice</code> (2102 J kg⁻¹ K⁻¹, the value at the melting point), matching the Community Firn Model&#39;s melt-enabled path. <code>:CuffeyPaterson</code> is <code>c_p(T) = 152.5 + 7.122·T</code> (Cuffey and Paterson, 2010, eq. 9.1); the constant form overstates the cold content of firn at 240 K by 12.9% and at 210 K by 27.5%.</p><p><strong>Internal energy is the enthalpy integral <code>∫c_p dT</code></strong>, not <code>M·T·c_p</code>. The latter is valid only for constant <code>c_p</code>; under <code>:CuffeyPaterson</code> it overstates enthalpy by <code>(b/2)T²</code>, which is 0.79·<code>L_f</code> at the melting point. Cell mixing, the thermal solver, and every energy budget work in enthalpy. Absolute column enthalpy is not comparable between the two methods (574 061 against 307 385 J kg⁻¹ at 273.15 K), so reported thermal energy shifts when switching.</p><p><strong>Thermal conductivity.</strong> <code>thermal_conductivity_method = :Calonne2019</code> is the default, Calonne et al. (2019) eq. 5 — a sigmoid blend at ρ = 450 kg m⁻³ between the Calonne (2011) snow quadratic and a firn branch, scaled by the temperature-dependent conductivity of ice. It is continuous into ice by construction and so is deliberately not short-circuited at <code>density_ice</code>; the <code>917</code> in its firn branch is the paper&#39;s fitted pure-ice density, not <code>mp.density_ice</code>. RetMIP Sect. 5.1 recommends it by name, and it is what both the Community Firn Model and IMAU-FDM ship.</p><p><code>:Calonne2019Air</code> additionally carries eq. 5&#39;s air-conductivity ratio on the snow branch, using Reid (1966) for air. See <a href="./imau_fdm_comparison">the IMAU-FDM comparison</a> for the magnitude. <code>:Marchenko2019</code> is Marchenko et al. (2019) eq. 30, <code>k = 0.301e-2·ρ − 0.724</code>, fitted over ρ = 350–900 and floored at the Calonne (2011) value below their crossing near ρ = 321, where the bare fit heads negative. Both <code>:Calonne2019</code> and <code>:Marchenko2019</code> are higher than <code>:Sturm</code>/<code>:Calonne</code> in firn, the direction RetMIP&#39;s cold bias at Summit and Dye-2 implies.</p><p>Face conductivity is the harmonic mean <code>1/(dz[i+1]/2K[i+1] + dz[i]/2K[i])</code> — the series resistance of the two half-cells, which is the exact flux for a piecewise-constant conductivity where an arithmetic mean is not.</p><p><strong>The explicit sub-step limit.</strong> The explicit solve is stable when each cell&#39;s own-temperature coefficient stays non-negative, <code>dt ≤ ρᵢcᵢdzᵢ/(Gᵢ + Gᵢ₋₁)</code>, where <code>Gᵢ</code> is the harmonic-mean face conductance the flux loop already uses. The Dirichlet bottom cell is excluded, its enthalpy never being updated. This is the limit for the graded grid GEMB actually solves on; the textbook uniform-grid form <code>0.5·ρᵢcᵢdzᵢ²/Kᵢ</code> substitutes <code>2Kᵢ/dzᵢ</code> for <code>Gᵢ + Gᵢ₋₁</code>, which agrees exactly only when <code>dz</code> and <code>K</code> are uniform. Because <code>Gᵢ + Gᵢ₋₁</code> ranges over <code>(0, 4Kᵢ/dzᵢ]</code>, the uniform form errs in both directions — measured per cell on GEMB&#39;s own column, the ratio spans 0.66 to 1.82. <code>thermal_explicit_safety_factor</code> (default 0.8) is not slack against an imprecise limit: <code>_max_safe_dt</code> bounds diffusion only, while the surface cell also carries the surface energy balance&#39;s feedback <code>Λ = dQ/dT₁ ≤ 0</code> in the same coefficient, and nothing in the model bounds <code>Λ</code>.</p><p><strong>Implicit surface-row damping.</strong> <code>ImplicitThermal</code>&#39;s Newton step weight is halved whenever a step fails to shrink, and the least-residual iterate is retained at the iteration cap. Without damping, 39% of sub-step solves reach the cap in limit cycles of ~0.02 K median amplitude straddling the latent-heat switch at 273.15 K, rather than in divergence; damping cuts that to 0.9% at no measurable runtime cost. See <a href="/GEMB.jl/previews/PR82/thermal_solvers#Thermal-solvers">Thermal solvers</a>.</p><h2 id="Turbulent-fluxes" tabindex="-1">Turbulent fluxes <a class="header-anchor" href="#Turbulent-fluxes" aria-label="Permalink to &quot;Turbulent fluxes {#Turbulent-fluxes}&quot;">​</a></h2><p><strong>Integrated stability functions.</strong> Paulson&#39;s (1970) closed forms on the unstable branch take the <em>inverse</em> profile function as their argument, so the exponents are positive: <code>x_m = (1 − 19ζ)^¼</code> inverts Högström&#39;s (1988) <code>φ_m = (1 − 19ζ)^(−¼)</code>, and likewise <code>x_h = (1 − 11.6ζ)^½</code>. Since <code>Ψ(ζ) = ∫₀^ζ (1 − φ)/z dz</code> must vanish at neutral stability, Högström&#39;s <code>κ_H/κ_M = 0.95</code> ratio belongs to the neutral transfer coefficient and not inside the integral — carried inside, it puts <code>Ψ_h(0) = −0.0999</code> and leaves the turbulent fluxes discontinuous exactly where <code>T_surface</code> crosses <code>T_air</code>. That crossing is where the implicit solver&#39;s Newton iteration converges, and where a jump can leave the surface energy balance with no solution in <code>T_surface</code> (Fourteau et al., 2024, Appendix D, who move their own branch point to <code>Ri_b = 0</code> for the same reason).</p><p>Both branches — this one and the stable Beljaars and Holtslag (1991) eqs. 28 and 32 — are checked against the definition directly, by numerically integrating their own published <code>φ</code> and comparing to the closed form, agreeing to 6 decimal places at <code>ζ = ±0.01, ±0.1, ±1, ±5</code>.</p><p><strong><code>ζ</code> is bounded below by −100 on the unstable branch.</strong> This is a numerical guard on the bulk formulation, not a change to the physics. <code>ζ</code> is diagnosed from the bulk Richardson number, which carries <code>wind_speed⁻²</code>, so at the <code>min_wind_speed = 0.01 m s⁻¹</code> floor over a melting surface under very cold air the synthetic forcing reaches <code>ζ ≈ −4.9e5</code>. That is the wind floor showing through rather than a stability regime, and Monin–Obukhov theory has no observational support anywhere near it (Högström&#39;s fits span <code>|ζ| ≲ 2</code>). Unbounded, <code>Ψ_h</code> grows past <code>log(z_T/z_Q)</code>, so <code>coefHT</code> crosses zero and the flux diverges and changes sign. −100 is two orders of magnitude beyond the calibration range, so it never binds in physically meaningful conditions, and it keeps the transfer coefficients positive for every roughness GEMB uses. Bounding <code>ζ</code> rather than clamping the coefficients keeps the fluxes a continuous, monotone function of <code>T_surface</code>, which the implicit solver needs.</p><h2 id="Meltwater" tabindex="-1">Meltwater <a class="header-anchor" href="#Meltwater" aria-label="Permalink to &quot;Meltwater {#Meltwater}&quot;">​</a></h2><p><strong>Irreducible saturation.</strong> <code>water_irreducible_method = :ColeouLesaffre</code> is the default: <code>S_wi = wmi/(1 − wmi)·ρᵢρ/(ρ_w(ρᵢ − ρ))</code> with <code>wmi = 0.057(ρᵢ − ρ)/ρ + 0.017</code>, Coléou and Lesaffre (1998) eq. 3 via Langen et al. (2017) eq. 4. The <code>1/(1 − wmi)</code> factor converts the paper&#39;s wet-mass fraction to a dry-mass basis. Retention is zero at and above pore close-off. Both the Community Firn Model and IMAU-FDM use this form, and RetMIP Sect. 5.4 attributes part of the multi-model under-retention in the percolation zone to a flat 0.07. <code>:constant</code> holds <code>water_irreducible_saturation</code> at every density, at every retention site.</p><p><strong>Melt geometry.</strong> <code>melt_geometry = :thickness</code> (default, as in Crocus) holds density fixed and shrinks <code>dz</code>; <code>:density</code> holds <code>dz</code> fixed and lowers density, as SNOWPACK does and as Fourteau et al. (2026) Sect. 2.3 argues for, on the grounds that the phase change occurs within the microstructure and that the high density of wet snow is better explained by its low viscosity under overburden. Refreezing is at constant thickness under both, so only <code>:density</code> returns a cell&#39;s geometry along with its mass over a melt–refreeze cycle. The choice affects melting cells&#39; <code>dz</code> and <code>density</code>, and through the irreducible-retention capacity their water and runoff.</p><p><strong>Impermeability.</strong> Water is routed to runoff at a contiguous run of cells at or above <code>impermeable_density</code> (default 830 kg m⁻³) thicker than <code>impermeable_thickness</code> (0.1 m). RetMIP recommends bucket schemes adopt such a criterion and finds model spread at ice-slab sites dominated by this pair; the participating models span 810 to 917 kg m⁻³. The <code>:ColeouLesaffre</code> retention gate stays on the constant <code>DENSITY_PORE_CLOSEOFF</code> — where capillary retention ceases for want of connected pore space is a different question from where a lens stops conducting flow — so lowering the flow criterion does not also change retention.</p><p><strong>Runoff timescale.</strong> <code>runoff_method = :instantaneous</code> is the default: all blocked water leaves within the timestep, which is what all three RetMIP bucket lineages and both comparison models do. <code>:ZuoOerlemans</code> drains <code>min(1, Δt/τ)</code> of the excess with <code>τ = c₁ + c₂·exp(−c₃·S)</code>, <code>c₁ = 0.33 d</code>, <code>c₂ = 25 d</code>, <code>c₃ = 140</code> (Zuo and Oerlemans, 1996, eqs. 21–22, coefficients via Lefebre et al., 2003 / Langen et al., 2017). <code>:Darcy</code> drains <code>min(excess, ρ_w·Δt·K_sat·K_rel·S)</code> with <code>K_sat</code> from Calonne et al. (2012) eq. 6 and <code>K_rel</code> from van Genuchten (1980) with the Yamaguchi et al. (2012) parameterization. Both read <code>surface_slope</code> as the hydraulic gradient. These are the schemes used by the two RetMIP models with the lowest firn-temperature error at the ice-slab site KAN_U (−1.6 °C and +0.6 °C, against a spread reaching +4.7 °C).</p><p>Two published lineages disagree on <code>c₁</code> — the Community Firn Model uses 1.5 d while citing the same source. GEMB follows Langen et al., the lineage RetMIP evaluated; the difference is immaterial at ice-sheet slopes (21 d either way at <code>S = 0.01</code>).</p><p><strong>Saturated pore space.</strong> Under <code>:instantaneous</code>, pore water is clamped to irreducible at every retention site, so a saturated cell cannot exist and firn aquifers are structurally unrepresentable — RetMIP Sect. 5.4 dropped their aquifer site from the retention evaluation for this reason. When <code>runoff_method !== :instantaneous</code>, water blocked at a barrier or the column base backs up into the pore space above it, filling each cell to <code>pore_saturation_max</code> of its capacity from the barrier upward; only what reaches past the surface cell runs off. Cells at or above <code>impermeable_density</code> are skipped as having no connected pore space. The upward pass does not refreeze — those cells&#39; cold content was consumed during percolation — and a cold cell left holding liquid is resolved by the refreeze-pore-water step on the next timestep.</p><p><strong>Rain sensible heat</strong> above the melting point is carried at <code>HEAT_CAPACITY_WATER = 4219.9 J kg⁻¹ K⁻¹</code> through the two-argument <code>specific_enthalpy_water(mp, T)</code>. Pore water is pinned at the melting point, where the one-argument form applies exactly. <code>rain_heat_capacity = :ice</code> carries it at <code>heat_capacity_ice</code> instead, which understates it by about a factor of two — ~3.9 kJ kg⁻¹ against 334.5 kJ kg⁻¹ of latent heat for rain at 275 K, growing with <code>T_air − 273.15</code>. On the synthetic site, 32-year melt differs by 48.7 kg m⁻² (0.43%).</p><p><strong>Percolation order.</strong> The downward flux is computed after refreezing, as in Crocus, and impermeability is re-tested after refreeze so a cell that has just become a barrier blocks water in the same timestep. See <a href="/GEMB.jl/previews/PR82/cfm_comparison#Differences-retained">the CFM comparison</a> for the alternative ordering and why no option is offered.</p><h2 id="Blowing-and-drifting-snow" tabindex="-1">Blowing and drifting snow <a class="header-anchor" href="#Blowing-and-drifting-snow" aria-label="Permalink to &quot;Blowing and drifting snow {#Blowing-and-drifting-snow}&quot;">​</a></h2><p><strong><code>blowing_snow_method = :Crocus</code></strong> [default <code>:none</code>] is the SURFEX/Crocus <code>SNOWDRIFT</code> scheme: a per-layer mobility index (Lafaysse et al., 2026, eqs. 59–61, the <code>B92</code> branch, which is the one written in dendricity, sphericity and grain size as GEMB&#39;s state is) and driftability index (eq. 62), an effective driftability decayed exponentially by the overburden above each layer (eq. 63), wind-slab densification toward 350 kg m⁻³ on a 2 d timescale (eq. 64), and grain fragmentation toward smaller, rounder, non-dendritic grains (eqs. 65–66). The layer loop stops at the first non-drifting layer, as the Crocus source does and eq. 63 does not show. It affects density, <code>dz</code>, grain radius, dendricity and sphericity, and through them albedo, conductivity, densification and percolation. It is <strong>exactly mass-conserving</strong> — <code>dz</code> shrinks in proportion to the density rise — so it appears in no mass budget.</p><p>Two departures from Crocus: GEMB has no wet-snow history variable, so the eq. 61 mobility cap uses instantaneous liquid water rather than &quot;has ever been wet&quot;, which leaves refrozen-but-now-dry layers more driftable; and melt-style thickness bookkeeping is not applied, the compaction being density-only.</p><p><strong><code>blowing_snow_sublimation</code></strong> [default <code>false</code>] sublimates the suspended fraction after Gordon et al. (2006) as Crocus implements it (eqs. 67–68), read only under <code>blowing_snow_method = :Crocus</code>. Off is Crocus&#39;s own default. This is the only part of the computed scheme that moves mass across the surface: it removes mass from the surface cell, capped at half of it per timestep, and reports it in <code>blowing_snow</code> and in the mass budget. <strong>No latent heat is charged to the column</strong> — the particles are suspended in the air and draw their heat of sublimation from it, unlike surface sublimation, which is charged. The Crocus paper (Lafaysse et al., 2026, Table K3) and its source disagree on which exponent belongs to the temperature ratio and which to the wind ratio; the source is followed, being the evaluated code, and the discrepancy is recorded in <code>src/blowing_snow.jl</code>.</p><p><strong><code>drift_rate</code></strong> [kg m⁻² yr⁻¹, default 0, bounded ±2000] and the optional <code>snow_drift</code> forcing layer are the prescribed path, positive for erosion. Drift is a surface mass source/sink with a sign-dependent density: eroded mass leaves at the surface cell&#39;s own density, deposited mass arrives at <code>fresh_snow_density</code> and at age zero, diluting the surface cell&#39;s age as condensation does. Taking the cell density in both directions would let an erode-and-redeposit cycle change surface density with no net mass exchange. Both are read independently of <code>blowing_snow_method</code>, so a regional-climate-model drift field can drive GEMB with no internal scheme; the forcing layer takes precedence where present.</p><h2 id="The-vertical-grid" tabindex="-1">The vertical grid <a class="header-anchor" href="#The-vertical-grid" aria-label="Permalink to &quot;The vertical grid {#The-vertical-grid}&quot;">​</a></h2><p>The column holds a <strong>constant cell count</strong> and a <strong>constant total depth</strong>, both fixed at initialization and enforced every timestep by two independent controllers: count is restored by exactly conservative merge and split operations at depth, and depth by a continuous signed adjustment to the bottom cell, the model&#39;s only basal mass and energy flux. Mass and energy are conserved throughout — checked every timestep under <code>verbose=true</code>, with the whole-run budget closing to ~1e-11 kg m⁻².</p><p>Consequences for consumers of the output:</p><ul><li><p><strong>Profile arrays are sized exactly to the column and top-justified</strong>, the surface always at row 1. There is no padding and no need to scan for <code>NaN</code>.</p></li><li><p><strong><code>column_depth_max</code> is a ceiling on the constructed column depth</strong>, not the depth itself. The depth is derived by <code>initialize_profile</code> from the climate and then held.</p></li><li><p><strong>Albedo is not carried state.</strong> All four methods (<code>:None</code>, <code>:GardnerSharp</code>, <code>:BrunLefebre</code>, <code>:GreuellKonzelmann</code>) are diagnostic functions of the current column, so <code>calculate_albedo</code> returns two scalars rather than per-cell vectors. The surface series <code>albedo_broadband</code> is recorded from the value used in that timestep&#39;s shortwave balance.</p></li><li><p><strong><code>ice_flux</code> is a signed per-interval basal flux</strong>, so <code>cumsum(ice_flux)</code> is the cumulative flux exactly. Its negation is surface elevation change against a datum fixed in the ice; see <a href="/GEMB.jl/previews/PR82/internals_grid#GEMB.trim_bottom!-Tuple{NamedTuple, Float64, ModelParameters}"><code>trim_bottom!</code></a> for the identity.</p></li></ul><p><strong><code>horizontal_strain_rate</code></strong> [yr⁻¹, default 0] is the trace of the horizontal strain-rate tensor <code>ε̇_xx + ε̇_yy</code>. By incompressibility it thins (positive, divergence) or thickens (negative, convergence) every cell at constant density by <code>exp(−D·dt)</code>. The mass it exports leaves laterally and is reported as <code>strain_thinning</code>, separately from the basal flux.</p><h2 id="Initialization-and-spinup" tabindex="-1">Initialization and spinup <a class="header-anchor" href="#Initialization-and-spinup" aria-label="Permalink to &quot;Initialization and spinup {#Initialization-and-spinup}&quot;">​</a></h2><p><strong>Deep temperature.</strong> The initialized deep temperature is the mean <em>surface</em> temperature, accumulated from the skin temperatures <code>_seb_annual_melt</code> already solves for, not the mean air temperature. The column is coupled to the atmosphere only through the surface energy balance, so its deep mean tends to the surface mean, and the two differ by the whole radiative and turbulent budget. This is not only an initialization detail: the deepest cell is a Dirichlet reservoir, so the initialized value is a boundary condition no length of spinup relaxes. Across a 21-site synthetic fleet the air-temperature value is biased +11.6 K warm on average and never cold (worst +24.5 K) against the self-consistent value, and the surface-temperature form cuts the mean temperature jump across that frozen cell from 0.79 K to 0.37 K. It also <em>increases</em> cycles to convergence (309 to 455 on that fleet): a slower-densifying colder column starts further from its density attractor. Refreezing&#39;s latent warming decays over the annual accumulation layer rather than being applied at every depth.</p><p><strong>No initialized cell starts above the melt point.</strong> <code>initialize_profile</code> clamps the temperature it fills to 273.15 K, with a warning. This binds only at sites whose mean annual air temperature is above freezing, where the column would otherwise begin as ice above its melting point holding no water — enthalpy the column has no state to carry.</p><p><strong>Age.</strong> <code>initialize_age = :steady_state</code> (default) carries the age <code>steady_state_profile</code> integrates along its march, the same march temperature, density and all three grain variables are initialized from. <code>:zero</code> starts every cell at zero, and the <code>constant_density</code> escape hatch forces <code>:zero</code> regardless, since it discards the march&#39;s density and the marched age describes a firn column that flag replaces with solid ice.</p><p>The <code>age</code> profile field is the mass-weighted mean age, in decimal days, of all mass in a cell (matrix plus pore water), measured from column initialization. Snowfall, rain and vapour deposition enter at age 0; proportional mass removal (melt, sublimation, runoff, basal trim under accumulation, horizontal strain) is age-neutral; merges mass-weight it and splits duplicate it. Meltwater carries the age of the firn it melted from and mass-weights that age into the cell where it refreezes, so a heavy melt year does not read as artificially young firn at depth. Age accumulates across <code>gemb_spinup</code> cycles, so for a spun-up run the epoch is the start of spinup. No physics reads it. Under sustained ablation the deepest cell&#39;s age is a lower bound, basal accretion inheriting that cell&#39;s own age — the same treatment <code>density</code> and <code>temperature</code> get at that site.</p><p><strong>Convergence.</strong> Spinup convergence is judged on the mass-weighted mean density of the whole column. Because the column depth is fixed for the run, the full column is the same domain at every cycle and the mean is exact without interpolation. <code>convergence_delta_density</code> bounds the change between consecutive cycles; <code>convergence_drift_density</code> bounds the least-squares slope of column-mean density against cycle over the trailing <code>drift_window</code> cycles (default 10), in kg m⁻³ per cycle. The trend test is the stricter claim, since a column creeping steadily at just under the delta tolerance passes the step test while still densifying. When both are given, both must hold.</p><p><strong>Spinup forcing.</strong> <code>forcing_climatology(method=:average)</code> (default) averages complete years into a one-year cycle. It preserves each field&#39;s mean but shrinks its variance by ~<code>1/n_years</code>, and melt is <em>rectified</em> — zero until the surface energy balance reaches the melt point — so averaging cancels the warm excursions that carry it. <code>forcing_climatology(method=:representative)</code> returns a block of <code>n_years</code> consecutive real years scored against the record instead.</p><p>Measured on ERA5-Land forcing at 12 glacierized Greenland sites (2000–2019), melt retained on the averaged cycle has a median of 12% of the record&#39;s, with 5 of 10 melting sites below 10%, against a median 102% for a 3-year representative block. The failure tracks elevation and absolute melt magnitude, not the melt/accumulation ratio: 0% retention at Saddle (2456 m) and DYE-2 (2094 m), 86–115% at the three lowest sites (271–577 m). High sites melt from the tail of the distribution, which averaging removes; low sites melt from the mean, which it preserves.</p><p>Averaging remains the default because it is right where melt is negligible: it preserves accumulation to 0.1–0.5% at the dry sites, against 15–22% for a melt-ranked block, and integrates less than half the model-years. <code>rank_by</code> selects the scoring variable (<code>:model</code> melt, <code>:smb</code> surface mass balance, <code>:estimate</code> the cheap surface-energy-balance melt), and <code>fallback_accumulation_tolerance</code> (default 0.05) re-selects on surface mass balance when a melt-ranked block&#39;s accumulation is too far off the record — measured to cut the worst accumulation error from 30.2% to 11.3% and the median from 6.9% to 2.5% across those sites.</p><p>Two limits are recorded rather than hidden: every figure above is <em>forcing</em> fidelity, since the equilibrated-column comparison could not be completed (6 of 12 sites do not converge in 800 cycles), and <code>:representative</code> costs about 2.2× the model-years of <code>:average</code>.</p><h2 id="Diagnostics" tabindex="-1">Diagnostics <a class="header-anchor" href="#Diagnostics" aria-label="Permalink to &quot;Diagnostics {#Diagnostics}&quot;">​</a></h2><p>These take no part in the mass or energy budget.</p><ul><li><p><strong><code>firn_air_content</code></strong> is metres of air, <code>Σ dz(1 − ρ/ρ_ice)</code>. <code>firn_air_content_10m</code> and <code>firn_air_content_20m</code> are the same integral to a fixed depth, which is how published values are reported (Vandecrux et al., 2019).</p></li><li><p><strong><code>percolation_depth</code></strong> is an interval maximum, matching what upward-looking radar measures (Heilig et al., 2018).</p></li><li><p><strong><code>ice_slab_thickness</code>, <code>ice_slab_depth</code>, <code>aquifer_thickness</code>, <code>aquifer_depth</code></strong> are instantaneous, so a <code>NaN</code> meaning &quot;no slab&quot; or &quot;no standing water&quot; cannot poison an interval mean. The slab terms are scanned after the grid controllers run, so recomputing slab depth from the output <code>dz</code> and <code>density</code> reproduces them. Aquifer detection is thresholded at <code>AQUIFER_TOLERANCE</code>, 1e-3 of a cell&#39;s pore space, rather than at the physics tolerance: <code>calculate_melt</code> leaves a retaining cell at exactly irreducible, but <code>calculate_density</code> then compacts it within the same timestep, shrinking the pore space retention was computed against and leaving a genuine ~1e-7-of-pore-space excess that accumulates between melt events.</p></li><li><p><strong><code>close_off_age</code></strong> is the age at the shallowest cell reaching pore close-off (830 kg m⁻³), <code>NaN</code> for an open column.</p></li><li><p><strong><code>heat_flux_basal</code></strong> is the conductive flux across the deepest interior face. It is a flux diagnosed against a Dirichlet reservoir, not a prescribed geothermal flux, and it tends to zero under a converged spinup — hence the name.</p></li><li><p><strong><code>viscosity</code></strong> [Pa s] is an optional profile layer under <code>output_viscosity = true</code>. It is populated by the <code>:Crocus</code>/<code>:CrocusPure</code> settling law and is <code>NaN</code> under every other scheme, which form no effective viscosity: they are <code>ρ̇ = c(ρᵢ − ρ)</code> relaxations with no stress in them, so a reconstructed <code>η = σ/(ρ̇/ρ)</code> would report a number derived from a stress the scheme never used.</p></li></ul>',74)])])}const m=t(i,[["render",s]]);export{p as __pageData,m as default};
